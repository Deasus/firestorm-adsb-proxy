{
  "name": "firestorm-adsb-proxy",
  "version": "1.0.0",
  "description": "Live ADS-B proxy for FIRESTORM — 5s-cached Vercel serverless function in front of airplanes.live + adsb.lol",
  "private": true,
  "engines": {
    "node": "20.x"
  }
}
